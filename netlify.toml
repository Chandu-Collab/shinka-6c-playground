[build]
  command = "npm run build"
  publish = ".next"

# Ensure Netlify uses the correct Node version
[build.environment]
  NODE_VERSION = "20"

# Explicitly configure the Next.js plugin
[[plugins]]
  package = "@netlify/plugin-nextjs"
